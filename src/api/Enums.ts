/**
 * 文件：src/api/Enums.ts
 * 职责：存放业务枚举定义。当前为示例枚举 SampleEnum，可作为后续枚举的模板。
 * 依赖：无
 * 导出：SampleEnum
 */

export enum SampleEnum {
  None = "None",
  Alpha = "Alpha",
  Beta = "Beta",
  Gamma = "Gamma",
}